# Question-2(STACKS AND QUEUES)
# parameter datatype is list, element datatype is integer, return datatype is integer
def queueins(data, x):
    data.append(x)
    return data

data_global = []

print(queueins(data_global, 3))
